node_modules
dist
.env
scripts/.binance.env
_to_delete/
.claude/

# Python bytecode from scripts/quant
__pycache__/
*.pyc

# Commit message drafts handed over for you to run
.commitmsg

# credentials for the overnight build runner — never committed
scripts/.build.env

# A source-only snapshot, tarred here so it can cross to the cloud container
# for a real build check. The repo is private, so cloning it there is no longer
# possible and this is the credential-free alternative.
_build-snapshot.tgz
_snap2.tgz
_snap5.tgz
_snap6.tgz
_snap7.tgz
_snap8.tgz
_snap4.tgz
_snap3.tgz

# Scratch patch scripts written by the assistant over the device bridge.
_*-patch.py
